name: node-release

on:
  push:
    tags:
      - "*"

jobs:
  publish:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mariadb:10.4
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: yes
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=30s --health-timeout=5s --health-retries=10

    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16.x
          registry-url: https://registry.npmjs.org/

      - run: npm i -g npm

      - run: npm ci
          
      - run: npm shrinkwrap

      - name: setup mysql user
        run: node ./docker/tidb/create_user.js

      - name: npm test
        run: npm test
        env:
          CI: true
          CDS_REQUIRES_DB_CREDENTIALS_HOST: "127.0.0.1"
          CDS_REQUIRES_DB_CREDENTIALS_PORT: 3306
          CDS_REQUIRES_DB_CREDENTIALS_DATABASE: cdstest
          CDS_REQUIRES_DB_CREDENTIALS_USER: cdstest
          CDS_REQUIRES_DB_CREDENTIALS_PASSWORD: cdstest

      - name: npm auth & publish
        run: npm run build && npm publish
        env:
          NODE_AUTH_TOKEN: ${{secrets.Z_NPM_TOKEN}}
